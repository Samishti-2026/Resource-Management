name: Backend CI/CD

on:
  push:
    branches:
      - vishal-prod
    paths:
      - 'backend/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Azure Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.PROD_ACR_LOGIN_SERVER }}
          username: ${{ secrets.PROD_ACR_USERNAME }}
          password: ${{ secrets.PROD_ACR_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build -t ${{ secrets.PROD_ACR_LOGIN_SERVER }}/backend:${{ github.sha }} .

      - name: Push Docker Image
        run: |
          docker push ${{ secrets.PROD_ACR_LOGIN_SERVER }}/backend:${{ github.sha }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.PROD_AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.PROD_AZURE_WEBAPP_PUBLISH_PROFILE }}
          images: ${{ secrets.PROD_ACR_LOGIN_SERVER }}/backend:${{ github.sha }}